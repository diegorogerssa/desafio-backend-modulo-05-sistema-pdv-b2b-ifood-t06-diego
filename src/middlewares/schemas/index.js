const schemaRegisterUser = require('./schemaRegisterUser');
const schemaLoginUSer = require('./schemaLoginUser');
const schemaCreateClient = require('./schemaCreateClient');
const schemaUpdateClient = require('./schemaUpdateClient');

const schemaRegisterProduct = require('./schemaRegisterProduct');

module.exports = {
  schemaRegisterUser,
  schemaLoginUSer,
  schemaCreateClient,
  schemaUpdateClient,
  schemaRegisterProduct

};
